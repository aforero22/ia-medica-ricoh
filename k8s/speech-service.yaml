apiVersion: v1
kind: Service
metadata:
  name: speech-service
  labels:
    app: speech-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: speech-service 