# Dockerfile para Frontend RAG con síntomas - Versión simplificada
FROM nginx:alpine

# Copiar archivo HTML
COPY frontend_rag.html /usr/share/nginx/html/index.html

# Configuración nginx básica
RUN echo 'server { listen 80; location / { try_files $uri $uri/ /index.html; } }' > /etc/nginx/conf.d/default.conf

# Exponer puerto
EXPOSE 80

# Comando por defecto
CMD ["nginx", "-g", "daemon off;"]
